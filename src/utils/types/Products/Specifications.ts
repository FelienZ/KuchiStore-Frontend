export interface ProductSpecifications {
  os: string;
  gpu: string;
  port: string;
  size: string;
  brand: string;
  camera: string;
  memory: string;
  display: string;
  network: string;
  storage: string;
  prosesor: string;
  resolution: string;
}